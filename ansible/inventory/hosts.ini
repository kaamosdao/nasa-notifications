# Пример хоста — ./run.sh спрашивает IP интерактивно и собирает временный inventory.
# Этот файл нужен только для ручных прогонов: ansible-playbook -i inventory/hosts.ini ...
[production]
prod-1 ansible_host=PROD_SERVER_IP ansible_user=root
