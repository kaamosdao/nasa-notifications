---
description: Render Strapi content safely: Blocks (the only wired, React-escaped path), rich text/HTML (normalizeRichText → RichTextRenderer, weak sanitizer), dynamic zones. Raw CMS HTML in dangerouslySetInnerHTML/html-react-parser is XSS.
globs:
alwaysApply: false
---
# Cms content rendering

Render Strapi content safely: Blocks (the only wired, React-escaped path), rich text/HTML (normalizeRichText → RichTextRenderer, weak sanitizer), dynamic zones. Raw CMS HTML in dangerouslySetInnerHTML/html-react-parser is XSS.

This is a pointer to the project skill. **Open and follow** `.claude/skills/cms-content-rendering/SKILL.md` in full —
it has the complete playbook with commands, patterns, and pitfalls.
