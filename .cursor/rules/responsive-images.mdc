---
description: Render CMS media via MediaImage + imgproxy: source/image/src props, MediaWithBreakpoints ({xs,sm,md,lg,default}), srcSet/sizes from the url, imgSizes(), the two breakpoint sets, MEDIA_FIELDS populate.
globs:
alwaysApply: false
---
# Responsive images

Render CMS media via MediaImage + imgproxy: source/image/src props, MediaWithBreakpoints ({xs,sm,md,lg,default}), srcSet/sizes from the url, imgSizes(), the two breakpoint sets, MEDIA_FIELDS populate.

This is a pointer to the project skill. **Open and follow** `.claude/skills/responsive-images/SKILL.md` in full —
it has the complete playbook with commands, patterns, and pitfalls.
