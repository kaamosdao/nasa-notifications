---
description: Strapi->frontend type bridge (manual): Zod model, requests via @strapi/client, the getServerSidePropsData orchestrator, getCommonData + cache, populate/pagination/draft.
globs:
alwaysApply: false
---
# Strapi frontend typing

Strapi->frontend type bridge (manual): Zod model, requests via @strapi/client, the getServerSidePropsData orchestrator, getCommonData + cache, populate/pagination/draft.

This is a pointer to the project skill. **Open and follow** `.claude/skills/strapi-frontend-typing/SKILL.md` in full —
it has the complete playbook with commands, patterns, and pitfalls.
Examples and docs: `@strapi/docs/`.
