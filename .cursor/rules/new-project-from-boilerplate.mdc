---
description: Spin up a new project from this boilerplate: rename (rebootme/boilerplate), set domains, generate Strapi env secrets, key hygiene.
globs:
alwaysApply: false
---
# New project from boilerplate

Spin up a new project from this boilerplate: rename (rebootme/boilerplate), set domains, generate Strapi env secrets, key hygiene.

This is a pointer to the project skill. **Open and follow** `.claude/skills/new-project-from-boilerplate/SKILL.md` in full —
it has the complete playbook with commands, patterns, and pitfalls.
