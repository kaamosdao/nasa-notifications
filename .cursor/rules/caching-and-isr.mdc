---
description: Caching + rendering strategy for server data (Next.js Pages Router + Strapi) — what to cache and for how long (global data vs catalog filtering vs per-request), short-TTL + single-flight for listings, moving content pages to ISR (getStaticProps + revalidate), and the Strapi revalidate webhook BY KEY (lru-cache invalidate + on-demand ISR path). Use when deciding whether to cache a request, picking a TTL, "should I cache catalog filtering", moving a page to ISR, wiring /api/revalidate, "stale header after CMS edit".
globs:
alwaysApply: false
---
# Caching & ISR

The decisions layer over server data: what to cache and for how long, SSR vs ISR, and how a Strapi edit
propagates (revalidate by cache key + on-demand ISR path). Mechanics of `cached()`/populate live in
`server-data-fetching`; this skill is the strategy.

This is a pointer to the project skill. **Open and follow** `.claude/skills/caching-and-isr/SKILL.md`.
Docs: `docs/11-server-cache.md`, `docs/12-sitemap.md`, `docs/07-strapi-preview.md`.
