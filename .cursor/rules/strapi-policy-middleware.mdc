---
description: Strapi 5 policies and middleware: policy (allow/deny) vs middleware (request/response wrapper), where they live and how they register, typical use cases.
globs:
alwaysApply: false
---
# Strapi policy middleware

Strapi 5 policies and middleware: policy (allow/deny) vs middleware (request/response wrapper), where they live and how they register, typical use cases.

This is a pointer to the project skill. **Open and follow** `.claude/skills/strapi-policy-middleware/SKILL.md` in full —
it has the complete playbook with commands, patterns, and pitfalls.
Examples and docs: `@strapi/docs/policy-middleware/`.
