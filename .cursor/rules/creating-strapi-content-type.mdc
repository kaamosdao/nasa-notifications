---
description: Create a Strapi entity: schema.json, field types, relations and components, access rights (API token/Public role), FE types via a Zod model.
globs:
alwaysApply: false
---
# Creating strapi content type

Create a Strapi entity: schema.json, field types, relations and components, access rights (API token/Public role), FE types via a Zod model.

This is a pointer to the project skill. **Open and follow** `.claude/skills/creating-strapi-content-type/SKILL.md` in full —
it has the complete playbook with commands, patterns, and pitfalls.
Examples and docs: `@strapi/docs/content-type/`.
