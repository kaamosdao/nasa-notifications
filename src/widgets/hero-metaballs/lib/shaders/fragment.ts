/**
 * Raymarching чёрного зеркального metaball на фоне звёздного неба.
 *
 * Одна процедурная функция `env()` даёт и фон, и то, что отражается в шаре, —
 * отражение согласовано с небом по построению, а не подгонкой цветов.
 */
export const FRAGMENT_SHADER = /* glsl */ `#version 300 es
precision highp float;

uniform vec2 uResolution;
uniform float uTime;
/** Позиция «курсорного» шара в мире (пружина считается на CPU). */
uniform vec3 uCursor;
/** 0 — указателя нет, шар вернулся на орбиту; 1 — тянется за курсором. */
uniform float uCursorActive;
uniform float uCamZ;
uniform float uScale;
uniform float uEnvIntensity;
uniform vec2 uParallax;
/** Сдвиг камеры в мире: в фоновой фазе уводит шар из-под ленты. */
uniform vec2 uOffset;

/**
 * Ручки формы и анимации. Пока вид подбирается, они приходят из dev-панели;
 * когда значения устоятся, их место — константы прямо здесь.
 */
uniform float uCore;
uniform float uBlend;
uniform float uOrbitCount;
uniform float uOrbitDistance;
uniform float uOrbitLift;
uniform float uOrbitRadius;
uniform float uSpeed;
uniform float uCursorRadius;

/** Вода: рябь на поверхности и то, сколько неба видно сквозь шар. */
uniform float uWaveAmp;
uniform float uWaveScale;
uniform float uWaveSpeed;
uniform float uRefraction;

/** Небо: общая яркость звёздного поля и количество межзвёздной пыли. */
uniform float uStarGain;
uniform float uDust;
uniform float uNebula;
uniform float uNebulaLife;
uniform float uTwinkle;

out vec4 fragColor;

#define FOV 0.8
#define MAX_STEPS 88
#define MAX_DIST 14.0
#define SURF_EPS 0.0015
/** Верхняя граница цикла: реальное число спутников задаёт uOrbitCount. */
#define MAX_ORBITS 6

/**
 * Нормаль плоскости Галактики. Наклон подобран так, чтобы полоса шла по кадру
 * диагональю, а не делила его пополам по горизонту.
 */
#define GALACTIC_NORMAL normalize(vec3(0.36, 0.86, -0.36))

float hash31(vec3 p) {
  p = fract(p * 0.3183099 + vec3(0.71, 0.113, 0.419));
  p *= 17.0;
  return fract(p.x * p.y * p.z * (p.x + p.y + p.z));
}

float noise3(vec3 x) {
  vec3 i = floor(x);
  vec3 f = fract(x);
  f = f * f * (3.0 - 2.0 * f);

  return mix(
    mix(
      mix(hash31(i + vec3(0.0, 0.0, 0.0)), hash31(i + vec3(1.0, 0.0, 0.0)), f.x),
      mix(hash31(i + vec3(0.0, 1.0, 0.0)), hash31(i + vec3(1.0, 1.0, 0.0)), f.x),
      f.y
    ),
    mix(
      mix(hash31(i + vec3(0.0, 0.0, 1.0)), hash31(i + vec3(1.0, 0.0, 1.0)), f.x),
      mix(hash31(i + vec3(0.0, 1.0, 1.0)), hash31(i + vec3(1.0, 1.0, 1.0)), f.x),
      f.y
    ),
    f.z
  );
}

/** Октавы параметром: небу нужен детальный шум, пыли хватает трёх слоёв. */
float fbm(vec3 p, int octaves) {
  float amplitude = 0.5;
  float sum = 0.0;

  for (int i = 0; i < octaves; i++) {
    sum += amplitude * noise3(p);
    p *= 2.02;
    amplitude *= 0.5;
  }

  return sum;
}

/**
 * Цвет абсолютно чёрного тела по температуре (аппроксимация Планка).
 * Спектральный класс — то, что отличает звёздное поле от россыпи белых точек:
 * холодные красные карлики массовы, горячие голубые единичны.
 */
vec3 blackbody(float kelvin) {
  float t = kelvin * 0.01;
  vec3 c;

  c.r = t <= 66.0 ? 1.0 : 1.292936 * pow(t - 60.0, -0.1332047);
  c.g = t <= 66.0
    ? 0.3900816 * log(t) - 0.6318414
    : 1.129891 * pow(t - 60.0, -0.0755148);
  c.b = t >= 66.0
    ? 1.0
    : (t <= 19.0 ? 0.0 : 0.5432068 * log(t - 10.0) - 1.196254);

  return clamp(c, 0.0, 1.0);
}

/**
 * Все свойства звезды из двух случайных чисел: масса и расстояние. Дальше —
 * только физика, поэтому цвет и яркость связаны, а не разыгрываются порознь:
 * красные карлики массовы и тусклы, голубые единичны и ослепительны. Разыграй
 * температуру и светимость независимо — и на небе появятся яркие красные звёзды,
 * которых в природе почти нет, а поле сразу читается как случайная россыпь.
 *
 * Возвращает поток (в потоках Солнца с единичного расстояния) и температуру.
 */
vec2 starPhysics(float um, float ud, float ug) {
  // Начальная функция масс Солпитера, dN/dM ~ M^-2.35, разыграна обратным
  // преобразованием: интеграл степени даёт M^-1.35, отсюда явная формула.
  // Константы — это pow(0.25, -1.35) и pow(24.0, -1.35), границы диапазона масс.
  float mass = pow(mix(6.499, 0.0137, um), -1.0 / 1.35);

  // Соотношение масса-светимость, кусочная аппроксимация: показатель меняется на
  // границах, где меняется способ переноса энергии внутри звезды.
  float lum = mass < 0.43
    ? 0.23 * pow(mass, 2.3)
    : (mass < 2.0 ? pow(mass, 4.0) : 1.4 * pow(mass, 3.5));

  // Радиус — эмпирика главной последовательности; температура из закона
  // Стефана-Больцмана L = 4 pi sigma R^2 T^4, записанного в солнечных единицах.
  float radius = pow(mass, 0.8);
  float kelvin = 5772.0 * pow(lum / (radius * radius), 0.25);

  // Гиганты. Звезда, сошедшая с главной последовательности, раздувается и остывает:
  // светимость вырастает на порядки, а поверхность краснеет. Их доля мала, но без
  // них поле остаётся бело-голубым — яркими на нём могут быть только горячие
  // звёзды, тогда как на настоящем небе самые заметные оранжевые как раз гиганты.
  if (ug < 0.014) {
    lum *= 240.0;
    kelvin = mix(3100.0, 4700.0, fract(ug * 83.0));
  }

  // Звёзды слоя лежат в объёме, а не на сфере: вероятность расстояния растёт как
  // d^2, поэтому d = u^(1/3). Без этого всё поле оказывается на одной дистанции,
  // и разброс видимых яркостей теряет главную свою причину.
  float dist = mix(0.4, 1.0, pow(ud, 1.0 / 3.0));

  return vec2(lum / (dist * dist), kelvin);
}

/** Полиномиальный smooth-min: «шейка» и отрыв шаров получаются из самой геометрии. */
float smin(float a, float b, float k) {
  float h = clamp(0.5 + 0.5 * (b - a) / k, 0.0, 1.0);
  return mix(b, a, h) - k * h * (1.0 - h);
}

float sdSphere(vec3 p, vec3 c, float r) {
  return length(p - c) - r;
}

float map(vec3 p) {
  float k = uBlend * uScale;
  float d = sdSphere(p, vec3(0.0), uCore * uScale);

  for (int i = 0; i < MAX_ORBITS; i++) {
    float fi = float(i);

    if (fi >= uOrbitCount) {
      break;
    }

    // Спутники расходятся по радиусу и скорости: с одинаковыми они выстраиваются
    // в кольцо и вращение читается как единая жёсткая деталь.
    float a = uTime * (0.22 + 0.06 * fi) * uSpeed + fi * 1.87;
    float dist = uOrbitDistance * (1.0 + 0.095 * fi);
    vec3 c = vec3(
      cos(a) * dist,
      sin(a * 0.7 + fi * 1.3) * uOrbitLift,
      sin(a) * dist
    ) * uScale;

    d = smin(d, sdSphere(p, c, uOrbitRadius * (1.0 - 0.1 * fi) * uScale), k);
  }

  // Без указателя шар чуть поджимается: на орбите он далеко и не должен спорить
  // с главным по массе.
  float cursorRadius = uCursorRadius * mix(0.667, 1.0, uCursorActive);

  d = smin(d, sdSphere(p, uCursor * uScale, cursorRadius * uScale), k * 0.9);

  return d;
}

vec3 calcNormal(vec3 p) {
  vec2 e = vec2(1.0, -1.0) * 0.0015;

  return normalize(
    e.xyy * map(p + e.xyy) +
    e.yyx * map(p + e.yyx) +
    e.yxy * map(p + e.yxy) +
    e.xxx * map(p + e.xxx)
  );
}

/**
 * Рябь. Геометрию не трогаем: волна на SDF стоила бы четырёх лишних fbm в каждом
 * шаге марша, тогда как на нормали её видно ровно так же — вся вода в кадре и так
 * читается только по бликам и по тому, как гуляет отражение.
 *
 * Из градиента убирается составляющая вдоль нормали: она бы просто гасила саму
 * нормаль, а наклонять её надо в касательной плоскости.
 */
vec3 waveNormal(vec3 p, vec3 n) {
  if (uWaveAmp <= 0.0) {
    return n;
  }

  // Шум течёт вдоль своей четвёртой оси, а не сдвигается целиком: сдвиг читался
  // бы как движение шара, а не как жизнь на его поверхности.
  vec3 q = p * uWaveScale + vec3(0.0, uTime * uWaveSpeed, uTime * uWaveSpeed * 0.6);

  float e = 0.35;
  float h = fbm(q, 2);
  vec3 grad = vec3(
    fbm(q + vec3(e, 0.0, 0.0), 2) - h,
    fbm(q + vec3(0.0, e, 0.0), 2) - h,
    fbm(q + vec3(0.0, 0.0, e), 2) - h
  ) / e;

  grad -= n * dot(grad, n);

  return normalize(n + grad * uWaveAmp);
}

/**
 * Один слой звёзд. Сетка трёхмерная и адресуется направлением луча, поэтому небо
 * бесконечно и не тайлится по экрану.
 *
 * Звезда здесь — точка на небесной сфере, а не диск в ячейке: расстояние до неё
 * меряется в радианах между направлениями, поэтому угловой размер задаётся явно
 * и не зависит ни от масштаба слоя, ни от разрешения канваса.
 *
 * right/up — экранный базис: по нему ориентированы дифракционные лучи, и
 * ориентация у всех звёзд общая, как у реального объектива.
 *
 * spread — угловой радиус ядра. Меньше пикселя звезду делать нельзя: она
 * начинает мерцать при малейшем движении камеры.
 *
 * clump — множитель плотности. Звёзды не рассыпаны равномерно, скучиваться их
 * заставляет диск Галактики.
 */
vec3 starLayer(
  vec3 d,
  vec3 right,
  vec3 up,
  float scale,
  float density,
  float spread,
  float clump
) {
  vec3 p = d * scale;
  vec3 cell = floor(p);

  if (hash31(cell) > density * clump) {
    return vec3(0.0);
  }

  // Разброс держим в пределах половины ячейки: соседние ячейки мы не перебираем,
  // и гало звезды у самой границы обрезалось бы швом.
  vec3 jitter = vec3(
    hash31(cell + 1.7),
    hash31(cell + 3.3),
    hash31(cell + 5.9)
  ) - 0.5;
  vec3 starDir = normalize(cell + 0.5 + jitter * 0.5);

  // Направления почти совпадают, поэтому хорда — уже сам угол, без acos.
  vec3 delta = starDir - d;
  vec2 t = vec2(dot(delta, right), dot(delta, up));
  float r = length(t);

  vec2 phys = starPhysics(
    hash31(cell + 11.13),
    hash31(cell + 23.71),
    hash31(cell + 37.53)
  );

  // Поток по полю различается в сотни тысяч раз, а экран отдаёт два порядка.
  // Сжимаем степенью — это та же логарифмическая шкала звёздных величин: порядок
  // яркостей сохраняется, физика в phys остаётся честной, а гало самой яркой
  // звезды перестаёт вылезать за ячейку и показывать её форму.
  float lum = min(uStarGain * pow(phys.x, 0.4), 6.0);

  // Мерцание. Это атмосферный эффект: воздух дрожит, и точечный источник дрожит
  // вместе с ним — протяжённые планеты как раз почти не мерцают. Период и фаза у
  // каждой звезды свои, иначе поле мигает разом, как лампа. Слабые звёзды на пике
  // проступают, а в провале исчезают совсем — отсюда ощущение живого неба.
  float tw = hash31(cell + 53.17);
  lum *= 1.0
    + uTwinkle
      * (0.3 + 0.7 * tw)
      * sin(uTime * mix(0.6, 2.4, tw) + tw * 31.4);

  // Размер ядра растёт с яркостью логарифмически: поток по полю различается на
  // семь порядков, а видимый диск звезды — на единицы пикселей.
  float mag = clamp(0.2 * log2(1.0 + lum), 0.0, 1.0);

  // Ядро — почти точка: даже самая яркая звезда занимает считанные пиксели.
  // Ниже пикселя опускаться нельзя, там начинается мерцание на движении камеры.
  float want = spread * (0.4 + 0.55 * mag);

  // Сверху размер ограничен ячейкой: звезда шире неё заливает ячейку целиком, и
  // вместо звёзд по небу идут кубы — ровно это видно в отражении, где spread
  // намеренно большой. Ячейка адресуется направлением, её угловой размер — 1/scale.
  float core = min(want, 0.2 / scale);

  // Размытие не добавляет света: звезда отдаёт яркость ровно в той мере, в какой
  // её не дали размазать. Поэтому в отражении остаются только самые яркие — так
  // размытое отражение звёздного поля и выглядит.
  lum *= (core * core) / (want * want);

  float ar = r / core;

  float disc = exp(-ar * ar * 1.6);
  // Гало затухает быстрее ячейки: иначе у ярких звёзд оно доживает до её границы,
  // и вместо звезды виден куб.
  float halo = 0.16 * exp(-ar * 0.9);

  // Крест виден только у ярких: у слабых он утонул бы в шуме и дал бы «сетку».
  vec2 a = abs(t) / core;
  float spikes = exp(-a.x * 0.5 - a.y * a.y * 0.7)
    + exp(-a.y * 0.5 - a.x * a.x * 0.7);
  spikes *= 0.09 * smoothstep(0.4, 1.0, mag);

  // Соседние ячейки мы не перебираем, поэтому всё, что доживает до границы,
  // гасится окном: иначе по небу идёт сетка швов. Ядро тоже — оно у самой яркой
  // звезды заметно шире точки.
  vec3 fc = abs(fract(p) - 0.5);
  float window = smoothstep(0.5, 0.3, max(fc.x, max(fc.y, fc.z)));

  // Цвет — не свободный параметр: он целиком определён температурой из physics.
  vec3 tint = blackbody(clamp(phys.y, 1900.0, 33000.0));

  return tint * lum * (disc + halo + spikes) * window;
}

/** Синус галактической широты: 0 — центр полосы, ±1 — полюса. */
float galacticLatitude(vec3 d) {
  return dot(d, GALACTIC_NORMAL);
}

/**
 * Система координат диска. Шум в ней сжат поперёк плоскости, иначе получаются
 * облака, а не полоса. Частота считается по всей сфере, а кадр охватывает лишь
 * её часть: на низких октавах в него попадает пара размытых пятен вместо
 * структуры диска.
 */
vec3 galacticFrame(vec3 d) {
  vec3 n = GALACTIC_NORMAL;
  vec3 tangent = normalize(cross(n, vec3(0.0, 0.0, 1.0)));
  vec3 bitangent = cross(n, tangent);

  return vec3(dot(d, tangent), dot(d, n) * 3.2, dot(d, bitangent)) * 11.0;
}

/**
 * Оптическая толща пыли на луче. Пыль живёт в диске, поэтому вне полосы её нет —
 * тёмные прожилки как раз и делают Млечный Путь узнаваемым, без них остаётся
 * просто светлое пятно.
 */
float dustDepth(vec3 q, float band) {
  return band * uDust * smoothstep(0.3, 0.72, fbm(q * 1.7 + 17.0, 3));
}

/**
 * Туманности. Два разных механизма свечения дают два разных цвета: ионизованный
 * водород излучает в линии H-альфа и потому малиново-красный, а холодная пыль
 * вокруг горячих звёзд просто рассеивает их свет и потому синяя — рассеяние тем
 * сильнее, чем короче волна. Смесь этих двух и читается как фиолетовый.
 *
 * Живут в диске: вне полосы газа и молодых звёзд нет.
 */
vec3 nebula(vec3 q, float band) {
  if (uNebula <= 0.0) {
    return vec3(0.0);
  }

  // Облако медленно течёт вдоль собственных осей, а не сдвигается целиком: сдвиг
  // читался бы как поворот всего неба. Скорость намеренно на грани заметности —
  // движение должно ощущаться, а не наблюдаться.
  vec3 flow = q + vec3(0.0, uTime * 0.011, uTime * 0.007) * uNebulaLife;

  // Масштаб крупнее, чем у пыли: туманность — область, а не прожилка.
  float mask = smoothstep(0.42, 0.86, fbm(flow * 0.42 + 41.0, 3));

  // Соотношение свечения и рассеяния гуляет, поэтому область дышит не только
  // яркостью, но и оттенком — от малинового к синему и обратно.
  float kind = smoothstep(
    0.36,
    0.64,
    fbm(flow * 0.27 + 91.0, 2) + 0.06 * sin(uTime * 0.19) * uNebulaLife
  );

  // Каждая область дышит в своей фазе: общая пульсация читалась бы как мигание
  // экрана, а не как жизнь внутри облака.
  float phase = fbm(q * 0.2 + 7.0, 2) * 24.0;
  float pulse = 1.0 + 0.3 * uNebulaLife * sin(uTime * 0.31 + phase);

  vec3 emission = vec3(0.40, 0.05, 0.26);
  vec3 scattering = vec3(0.09, 0.12, 0.44);

  return mix(emission, scattering, kind)
    * mask
    * pulse
    * pow(band, 0.7)
    * uNebula;
}

/** Свечение диска. Приходит уже ослабленным: пыль лежит и перед ним тоже. */
vec3 milkyWay(vec3 q, float band, vec3 transmittance) {
  float glow = fbm(q + 3.1, 4);
  float body = band * (0.25 + 1.0 * glow);

  return mix(vec3(0.006, 0.0062, 0.011), vec3(0.016, 0.014, 0.024), glow)
    * body
    * transmittance;
}

/**
 * Небо целиком: фон и содержимое отражения — одна и та же функция.
 *
 * spread — угловой радиус самой мелкой детали. Для фона это пиксель, для
 * отражения — заметно больше: кривизна шара сжимает в один пиксель целый кусок
 * неба, и точечные звёзды там кипели бы на каждом кадре.
 */
vec3 env(vec3 rd, float spread) {
  vec3 d = normalize(rd + vec3(uParallax * 0.06, 0.0));

  // Опорную ось меняем у полюса: cross с почти сонаправленным вектором вырожден.
  vec3 ref = abs(d.y) < 0.95 ? vec3(0.0, 1.0, 0.0) : vec3(0.0, 0.0, 1.0);
  vec3 right = normalize(cross(d, ref));
  vec3 up = cross(right, d);

  float lat = galacticLatitude(d);
  float band = exp(-lat * lat * 60.0);

  vec3 q = galacticFrame(d);

  // Закон Бугера-Ламберта. Коэффициент поглощения растёт к синему концу спектра,
  // поэтому за пылевой полосой звёзды не просто тусклее, а заметно рыжее — это
  // покраснение и есть главный признак того, что пыль настоящая, а не тёмный
  // мазок поверх картинки.
  vec3 tau = dustDepth(q, band) * vec3(0.62, 0.86, 1.32);
  vec3 transmittance = exp(-tau);

  vec3 col = mix(vec3(0.0012, 0.0016, 0.004), vec3(0.002, 0.0025, 0.006), band);

  // Свечение диска размазано по всей его толще, поэтому перед ним в среднем лишь
  // часть пылевого столба.
  col += milkyWay(q, band, exp(-tau * 0.5));
  col += nebula(q, band) * exp(-tau * 0.5);

  // К полосе звёзды сгущаются, вдали от неё поле заметно реже.
  float clump = 0.5 + 1.9 * band;

  // Чем дальше слой, тем длиннее путь сквозь пыль: ближние звёзды гаснут слабо,
  // дальние — полностью.
  col += starLayer(d, right, up, 26.0, 0.14, spread, clump)
    * pow(transmittance, vec3(0.35));
  col += starLayer(d, right, up, 57.0, 0.11, spread, clump)
    * 0.8 * pow(transmittance, vec3(0.7));
  col += starLayer(d, right, up, 119.0, 0.08, spread, clump) * 0.55 * transmittance;

  return col;
}

/** Мягкое сжатие светов: ядро яркой звезды уходит в белый, гало сохраняет цвет. */
vec3 tonemap(vec3 x) {
  return x / (1.0 + x);
}

void main() {
  vec2 uv = (gl_FragCoord.xy * 2.0 - uResolution) / uResolution.y;

  vec3 ro = vec3(uOffset, uCamZ);
  vec3 rd = normalize(vec3(uv * FOV, -1.0));

  // Угловой размер пикселя. Считается от буфера, а не от CSS-размера, поэтому
  // при деградации качества звёзды укрупняются вместе с ним и не начинают рябить.
  float pixelAngle = 2.0 * FOV / uResolution.y;

  float t = 0.0;
  bool hit = false;

  for (int i = 0; i < MAX_STEPS; i++) {
    float d = map(ro + rd * t);

    if (d < SURF_EPS * t) {
      hit = true;
      break;
    }

    t += d * 0.9;

    if (t > MAX_DIST) {
      break;
    }
  }

  vec3 col;

  if (hit) {
    vec3 p = ro + rd * t;
    vec3 n = waveNormal(p, calcNormal(p));
    vec3 r = reflect(rd, n);

    // Френель по Шлику, F0 = 0.02 — это ровно вода: в центре сквозь поверхность
    // видно объём, к краям она превращается в зеркало.
    float facing = clamp(dot(-rd, n), 0.0, 1.0);
    float fresnel = 0.02 + 0.98 * pow(1.0 - facing, 5.0);

    // Отдельного источника света в сцене нет: всё, что видно на поверхности, —
    // отражённое и преломлённое небо. Блик от выдуманного «студийного» источника
    // стоял здесь раньше и был единственным местом, где свет не следовал из
    // окружения; на ряби он к тому же размазывался в пятно.

    // Небо сквозь толщу: показатель преломления воды и поглощение красного —
    // отсюда сине-зелёный цвет, а не тонировка на глаз.
    vec3 through = env(refract(rd, n, 1.0 / 1.333), pixelAngle * 9.0);
    through *= vec3(0.25, 0.62, 0.75) * uRefraction;

    col = vec3(0.002, 0.003, 0.005);
    col += through * (1.0 - fresnel) * uEnvIntensity;
    col += env(r, pixelAngle * 7.0) * fresnel * uEnvIntensity;
    col += vec3(0.16, 0.22, 0.42) * pow(1.0 - facing, 3.0) * 0.35 * uEnvIntensity;
  } else {
    col = env(rd, pixelAngle);
  }

  col *= 1.0 - 0.09 * dot(uv, uv);
  fragColor = vec4(pow(tonemap(max(col, 0.0)), vec3(0.4545)), 1.0);
}
`;
